# Dependencias de desarrollo. Instalar sobre requirements.txt:
#     pip install -r requirements.txt -r requirements-dev.txt

# --- Tests ---
pytest>=8.3,<9.0
pytest-cov>=5.0,<7.0
httpx>=0.27,<1.0          # requerido por fastapi.testclient

# --- Calidad de código ---
ruff>=0.6,<1.0            # linter + formateador
mypy>=1.11,<2.0

# --- Notebooks ---
jupyter>=1.1,<2.0
ipykernel>=6.29,<7.0
